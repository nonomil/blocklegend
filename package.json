{
  "name": "blocklegend",
  "version": "0.9.4",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "我的方块学园 · 地下城闯关，边打边学",
  "scripts": {
    "start": "npx --yes serve . -p 4173",
    "android:prepare": "node scripts/prepare-mobile.mjs",
    "android:init": "npm run android:prepare && cap add android && node scripts/patch-android.mjs",
    "android:sync": "npm run android:prepare && cap sync android && node scripts/patch-android.mjs",
    "android:build": "npm run android:sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "8.4.2",
    "@capgo/capacitor-speech-recognition": "8.0.10"
  },
  "devDependencies": {
    "@capacitor/android": "8.4.2",
    "@capacitor/cli": "8.4.2"
  }
}
