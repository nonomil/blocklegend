# 图解（Mermaid）

## 1. 本周期工作全景（已完成 → 待做）

```mermaid
flowchart LR
  subgraph done["✅ 本周期已完成(28/28测试绿)"]
    A[模块化 world-gen<br/>+测试基线0→28] --> B[树木贴图<br/>5色/树冠3层/叶色]
    B --> C[tileset无缝化<br/>dirt/stone/sand 0接缝]
    C --> D[氛围:15气候雾色/光色]
    D --> E[体素云层<br/>1 draw call]
    E --> F[骑龙全套:急速/回旋/龙息<br/>尾迹/键冲突修复/HUD]
  end
  F --> G
  subgraph todo["📋 待做"]
    G[P0 骑龙大世界Phase1<br/>HEIGHT_MAX 48/山系噪声] --> H[P1 龙息×语音暴击联动<br/>水面/相机]
    H --> I[P2 大地标/草摆动]
    I --> J[P3 扩图(真机帧率后议)]
  end
```

## 2. 骑龙大世界三阶段依赖

```mermaid
flowchart TD
  P1["Phase1 拔高+山系<br/>HEIGHT_MAX 48 · 脊谷噪声 · 高空雾"] --> T1{"MuMu真机<br/>帧率劣化≤15%?"}
  T1 -- 否 --> R1[降振幅±20→±14<br/>或减云密度] --> P1
  T1 -- 是 --> P2["Phase2 大地标<br/>每气候1个30+格锚点"]
  P2 --> P3["Phase3 扩图384→512<br/>或边缘高山无边感"]
  T1 -- 严重超标 --> STOP[回退Phase1<br/>重评估方案]
```

## 3. 骑龙输入链路（修复后）

```mermaid
flowchart LR
  K[键盘 R/Q/E] --> RK[engine refreshKeys:1308<br/>input.boost/rollL/rollR]
  RK --> MP[updateMountPhysics<br/>speedFactor 1.7x / rollT 0→1]
  MP --> TL[tickLife:1463<br/>mesh.rotation.z = a.roll]
  TL --> FV[fourViewModel tick<br/>翅膀12Hz扇动]
  K2[左键] --> BM[game.js beginMine<br/>riding? → tryDragonBreath]
  BM --> FBS[fireBossShot<br/>fireball·track·friendly]
  FBS --> MBS[moveBossShots<br/>hurtMonster 4 → HUD伤害数字]
  K3[Q/E 骑乘时] --> GUARD[game.js:877 riding守卫<br/>不触发tryBolt/tryEat]
```
