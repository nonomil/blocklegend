# 测试分析报告（2026-08-20）

> 结论先行：**28/28 绿** + 无头浏览器冒烟 0 错误。测试体系本周期从 0 → 28 条，覆盖数据层回归锚；已知盲区如实列出。

## 一、现状

| 套件 | 条数 | 守护对象 |
|---|---|---|
| test/world-gen.test.mjs | 6 | makeRng/hash3 确定性、createWorld 结构、heights 深比较、biomeAt 合法、openWordGate 可调、橡树多层冠 |
| test/atlas.test.mjs | 7 | CORE 8 块 16×16 像素、非叶全不透明、paintCore 喂像素数、grass_top ≤4 色、4 叶镂空一致、橡叶 5 色上亮下暗 |
| 其余新增 | 15 | 子代理/其它会话落盘（含 tileset 无缝化守护）——**作者未逐一核对** 🔲 |

命令：`node --test "test/*.test.mjs"`（glob 必需）。

## 二、本周期测试事件

1. **11 → 28**：中途磁盘出现 +10 条（会话外落盘，测试全绿，未回滚）
2. **atlas 测试两次修正**：oak_leaf 有 alpha 镂空（全不透明断言错）；LEAF_TINT 未导出（改结构断言）——教训：断言前先读实现的真实导出面
3. **grok 审查暴露的盲区已补**：橡叶长相锁进测试（5 色/25 洞/上亮下暗）；W 空值守卫

## 三、已知盲区（Phase 1 前须知）

| 盲区 | 影响 | 补法建议 |
|---|---|---|
| **heights 深比较锚会合法失效** | Phase 1 改振幅必破 | 更新期望前先人工核对新地形（E2 截图） |
| 树冠占格无回归锚（grok P2 指出） | 每TreeVoxel 改动无人守护 | 加 treeCols 断言（树干列集合稳定） |
| 运行时闭包（合批/物理/UI）零测试 | engine/game.js 回归靠人工 | 维持现状（闭包不可 node 测），靠冒烟脚本 |
| 移动端/WebView 无自动化 | MuMu 上只有人工 | 真机手测清单：起飞/穿云/回旋/龙息/帧率 |

## 四、验证金字塔（本周期实践，接手者沿用）

```
L1  node --check ×N 文件            （秒级，每编辑后）
L2  node --test "test/*.test.mjs"    （数据层回归）
L3  无头 Chrome+CDP 冒烟             （boot 零异常 + 关键全局就位）
L4  浏览器人工操作验收                （手感/观感，需用户）
L5  MuMu 真机                        （帧率红线，Phase 1 必做）
```
冒烟脚本：`C:\Users\No'mi'l\AppData\Local\Temp\claude\bl-smoke.mjs`（临时目录，可能被清——核心是 node v22 内建 WebSocket 连 CDP Runtime.evaluate，可随时重建）。
