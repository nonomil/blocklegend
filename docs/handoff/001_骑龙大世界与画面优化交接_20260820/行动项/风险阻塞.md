# 风险与阻塞

## 🔴 阻塞（影响工作方式）

| # | 阻塞 | 影响 | 绕行方案 |
|---|---|---|---|
| B1 | **子代理/executor API 401 余额不足**（opencode.ai） | 后台并行委派不可用（本周期 3 个代理中途死） | 主上下文串行做；充值后先跑 E6 验证再恢复 |
| B2 | WebSearch 同 401 | 联网调研不可用（MC 世界生成机制只能靠既有知识） | 用户侧检索或充值后补；方案已标注"依据为既有知识 🔲建议核实" |
| B3 | **4196 端口僵尸**（3 PID 叠占，路径 404） | 双击 bat 打开的游戏可能加载不到资源 | 先 `netstat -ano | findstr :4196` 杀全 PID 再启 bat；开发验证用自起 8642 |

## 🟡 风险（Phase 1 执行时关注）

| # | 风险 | 缓解 |
|---|---|---|
| R1 | HEIGHT_MAX 48 → 每区块三角形上升，真机帧率掉 | E1 基线先行；振幅可调（±20→±14）；MOBILE_VIEW_CHUNKS=2 已是手机挡位 |
| R2 | 测试 heights 基线破坏 | 合法更新期望；更新前先人工确认新地形正确再改测试（别让测试迁就 bug） |
| R3 | 陡坡上树/建筑悬空 | 种树限坡度阈值；建筑复用 flattenPad |
| R4 | 无 git 锚 + 多会话曾并发改盘 | 动手前 Read 最新盘面；重要节点手动快照（P3-11） |
| R5 | 洞穴/河流 stamp 在高差 48 下的行为未知 | A2 完成后专门肉眼过一遍洞穴入口与河岸 |

## 🟢 已解除

- ~~龙息缺失~~（主上下文补完，28/28 绿 + 冒烟 0 错误）
- ~~Q/E 键冲突~~（riding 守卫）
- ~~云层未建~~（ensureCloudLayer 落地）
- ~~CliproxAPI 生图依赖~~（dragon-atlas 已存在，无需生图）
