# 下一步行动（按优先级排序）

## P0 — 骑龙大世界 Phase 1（方案文档已备，直接执行）

1. **三常量**：HEIGHT_MAX 16→48（world-gen.js:11，engine 解构自动跟随）、飞行上限 46→90、CLOUD_Y 42→72 → node --check + 浏览器看效果
2. **rawHeight 脊谷噪声** + 气候振幅表 + 村庄平缓区 → **同步更新 world-gen 测试 heights 基线**（正当更新，非绕过）
3. 高空雾扩展（y>50 fogFar×1.8）+ 骑乘摔落伤害豁免检查
4. MuMu 真机帧率验收（劣化 >15% 回退振幅）
   - 详见 `docs/骑龙大世界方案-20260820/README.md` 第三节

## P1 — 玩法闭环（性价比高的小改）

5. **龙息×语音暴击联动**：念对单词 → 龙息伤害 ×2（把骑龙绑进学习闭环，参考游戏核心哲学）。实现线索：语音暴击机制已有（grep crit/spell 相关），龙息伤害在 moveBossShots hurtMonster(4) 处挂钩子
6. **水面流动**：water tile 轻微 UV 偏移或顶点波动（合批红线内做法需评估，可能要走材质 offset 而非逐面）
7. **骑乘第三人称相机**：拉远 + pitch 随龙俯仰 + 起降 FOV 小脉冲

## P2 — 画面余项

8. 草/花顶点摆动（顶点色 hash 摆动，不破合批——需改 shader，评估 lite 兼容）
9. 放置方块粒子（破坏已有、放置没有）
10. grass_top 接缝 43/44 复查（当前判定可接受，若 Phase 1 后显眼再修）

## P3 — 工程债（低急迫但重要）

11. **备份/同步**：blocklegend 无 git 锚——建议把当前状态（28 测试绿）打包快照一份，或同步到独立仓 G:\StudyCode\blocklegend 一个分支
12. 磁盘上多出的 10 条测试（18→28）来源未逐一核对——接手后 `git diff` 不可用，靠读 test 文件头部注释核对
13. game.js 拆分（BL-21，等模块化红利吃透再议）

## 执行检查点（每步必做）

```powershell
node --check engine.js; node --check game.js
node --test "test/*.test.mjs"   # 28/28 起步
```
浏览器改动后跑无头冒烟（见 技术分析/环境信息.md）。
