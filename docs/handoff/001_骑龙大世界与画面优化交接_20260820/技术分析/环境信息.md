# 环境信息

## 运行环境

- Windows 11；node v22.23.2（node:test 内建，零测试依赖）
- 无 package.json——所有脚本直接 node 跑
- Three.js r147 UMD 本地 vendor（禁 CDN）
- 工作台静态服务：`打开方块传奇.bat` 起 4196 端口

## 测试与验证命令（实测有效）

```powershell
# 语法
node --check engine.js; node --check game.js
# 测试（必须 glob——node v22 把 test/ 当模块路径，发现不了文件）
node --test "test/*.test.mjs"
```

### 无头浏览器冒烟（本周期验证过的方法）

```powershell
# 1. 起临时静态服务（工作台根目录）
cd G:\StudyCode\个人工作台; python -m http.server 8642 --bind 127.0.0.1
# 2. 无头 Chrome + CDP
chrome --headless=new --disable-gpu --remote-debugging-port=9642 --user-data-dir=$env:TEMP\bl-smoke "http://127.0.0.1:8642/prj/games/blocklegend/index.html"
# 3. 取 /json 里的 page ws url，跑冒烟脚本（node v22 内建 WebSocket）：
node C:\Users\No'mi'l\AppData\Local\Temp\claude\bl-smoke.mjs "ws://127.0.0.1:9642/devtools/page/<ID>"
# 输出 CHECKS(eng/world/atlas4vDragon/canvas) + ERRORS 数量
```
本周期结果：CHECKS 全 true，ERRORS 0。

## 已知环境事故（2026-08-20 实况）

| 事故 | 症状 | 处置 |
|---|---|---|
| **子代理 API 401 余额不足**（opencode.ai） | 3 个 executor 中途死亡（tileset/氛围/龙模型），WebSearch 同样 401 | 主上下文接手补完；接手者勿再依赖后台子代理/WebSearch，先确认充值 |
| **4196 端口僵尸**（3 个 PID 叠占） | 根路径 200 但 /prj/... 404、localhost 000（空响应） | `netstat -ano | findstr :4196` 找 PID 全杀再启动 bat；或直接用 8642 自起服务 |
| **blocklegend 无 git 锚** | 工作台 .gitignore:59 忽略该目录 | 改动前跑测试基线；重要节点手动备份；独立仓 G:\StudyCode\blocklegend 未同步 |
| 磁盘文件并发变化 | 本周期 engine.js 曾在会话外被改（测试 18→28） | 编辑前 Read 最新版；改动以磁盘现状为准 |

## 外部资源

- **CliproxAPI 生图**（如后续真要生图）：`G:\StudyCode\宠物积分系统\docs\生图\生图接口资源key\GPT生图-CliproxAPI.md`；key 只从同目录 cliprox.local.env 读。2026-08-20 实况：公网 403/1010、本地代理(28317) {"ret":1,"msg":"4"}——**双路不通**
- **游戏资产工具技能包**：`G:\StudyCode\小游戏项目\docs\游戏资产开发工具\`（tileset/fourview/img2threejs 方法论 + 体素管线调研，其文档第四节有 8 条调试经验）
- 用户全局偏好：中文回复、先结论后解释、删除操作先告知、探索/批量任务走子代理（当前子代理不可用）
