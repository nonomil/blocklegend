# Phase 1：做通前 10 分钟

> **给 Claude:** 必需子技能：使用 executing-plans。先完成 Phase 0。

> **历史状态：已实施。** 本文件保留当时的红绿步骤；战斗触发、Listening 和 Boss 开口方式已被 [Phase 5](./2026-08-16-05-voice-world-loop.md) 覆盖，不能按旧步骤回退现有体验。

**目标：** 第一次进入只看见一个目标；答错能学会；第一关只盯 5 个词；Boss 要其中 3 个词破盾。

**架构：** 任务状态机做成纯函数 `data/quests.js`。词池目标与纠错做成 `data/words.js` 纯函数。`game.js` / `index.html` 只装配 HUD 与事件。

**技术栈：** 现有 IIFE + Node test

---

### 任务 1.1：关卡 1 分步任务状态机（BL-01）

**文件：**
- 创建：`prj/games/blocklegend/data/quests.js`
- 修改：`prj/games/blocklegend/index.html` 加入脚本
- 测试：`tests/blocklegend.test.mjs`

**步骤 1：写失败测试**

```js
await import('../prj/games/blocklegend/data/quests.js');
const Q = globalThis.BlockLegendQuests;

test('level 1 quest starts at look-tree and advances one event at a time', () => {
  const q = Q.create(1);
  assert.equal(Q.current(q).id, 'look-tree');
  assert.match(Q.current(q).title, /树|tree/i);
  const afterLook = Q.apply(q, { type: 'look', kind: 'log' });
  assert.equal(Q.current(afterLook).id, 'make-sword');
  const afterCraft = Q.apply(afterLook, { type: 'craft', id: 'wood_sword' });
  assert.equal(Q.current(afterCraft).id, 'hit-slime');
  const afterKill = Q.apply(afterCraft, { type: 'kill', kind: 'slime', quizCorrect: true });
  assert.equal(Q.current(afterKill).id, 'learn-five');
  const afterWords = Q.apply(afterKill, { type: 'word-correct', count: 5 });
  assert.equal(Q.current(afterWords).id, 'break-boss');
  const done = Q.apply(afterWords, { type: 'boss-shield-break' });
  assert.equal(done.complete, true);
});
```

**步骤 2：跑测试，期望 `BlockLegendQuests` 未定义**

**步骤 3：最小实现 `create` / `current` / `apply`，步骤 id 固定为：**

1. `look-tree` — 准星看原木
2. `make-sword` — 合成木剑
3. `hit-slime` — 答对并打史莱姆
4. `learn-five` — 本关目标词答对 5 个
5. `break-boss` — 破一次 Boss 罩

无关卡 2–6 先返回 `{ complete: true, steps: [] }` 或单步「完成本关」。

**步骤 4：测试通过；`index.html` 在 `levels.js` 后加载 `quests.js`

---

### 任务 1.2：首屏只显示当前目标（BL-02）

**文件：**
- 修改：`prj/games/blocklegend/index.html`
- 修改：`prj/games/blocklegend/game.css`
- 修改：`prj/games/blocklegend/game.js`
- 测试：`tests/blocklegend.test.mjs`

**步骤 1：写失败测试**

```js
test('default hud shows a single quest goal node', () => {
  const html = fs.readFileSync(path.join(repoRoot, 'prj', 'games', 'blocklegend', 'index.html'), 'utf8');
  assert.match(html, /id="quest-goal"/);
  assert.match(html, /id="quest-hint"/);
});
```

**步骤 2：失败后在 RPG 面板顶部加：**

```html
<p class="bl-quest" id="quest-panel">
  <b id="quest-goal">找到会发光的橡树</b>
  <span id="quest-hint">WASD 走到树前，对准它</span>
</p>
```

`game.js` 在 `syncHud` 里用 `Q.current(session.quest)` 更新这两行。

完整操作 hint 在首次锁定鼠标后淡出（已有 `look-tip`）。Listening 按 Phase 5 改为紧凑常驻的 `Say: focus words` + `My English: N words`；不要重新把它整体隐藏。

**步骤 3：测试通过**

---

### 任务 1.3：答错纠错与一次免罚重试（BL-03）

**文件：**
- 修改：`prj/games/blocklegend/data/words.js`
- 修改：`prj/games/blocklegend/game.js` 答题结算
- 测试：`tests/blocklegend.test.mjs`

**步骤 1：写失败测试**

```js
test('wrong quiz answer offers one free retry and does not mark mastery yet', () => {
  const quiz = W.makeQuiz({ text: 'tree', zh: '树', id: 'tree' }, []);
  const first = W.resolveAttempt(quiz, '花');
  assert.equal(first.correct, false);
  assert.equal(first.retry, true);
  assert.equal(first.record, false);
  assert.equal(first.reveal, '树');
  const secondWrong = W.resolveAttempt(quiz, '花', { retried: true });
  assert.equal(secondWrong.retry, false);
  assert.equal(secondWrong.record, true);
  assert.equal(secondWrong.correct, false);
  const recovered = W.resolveAttempt(quiz, '树', { retried: true });
  assert.equal(recovered.correct, true);
  assert.equal(recovered.crit, true);
  assert.equal(recovered.comboKeep, false);
});
```

**步骤 2：失败后实现 `resolveAttempt(quiz, input, opts)`**

规则：

- 首次错：`retry=true`，`record=false`，给出 `reveal`（正确答案）和 `speak=true`
- 重试对：`correct=true`，给正常暴击，不涨连击（`comboKeep=false`）
- 重试错：`record=true`，普通伤害

**步骤 3：`game.js` 答题回调改走 `resolveAttempt`，错后高亮正确答案并留在题卡**

**步骤 4：测试通过**

---

### 任务 1.4：第一关 5 词目标池（BL-06）

**文件：**
- 修改：`prj/games/blocklegend/data/words.js`
- 修改：`prj/games/blocklegend/data/levels.js`（level 1 加 `targetWords: 5`）
- 测试：`tests/blocklegend.test.mjs`

**步骤 1：写失败测试**

```js
test('level 1 focuses five world words, not a 100-word exam pool', () => {
  const focused = W.focusPool(bank, 1, { size: 5, prefer: ['tree', 'dirt', 'sword', 'slime', 'apple'] });
  assert.equal(focused.length, 5);
  focused.forEach((w) => assert.ok(w.text && w.zh));
});
```

**步骤 2：实现 `focusPool(bank, level, opts)`**

- 先按 `prefer` 文本从 bank 取
- 不足则从 `poolForLevel` 补齐
- **不要改** `poolForLevel` 的 6 等分行为（现有测试依赖）

**步骤 3：`startLevel(1)` 的出题池用 `focusPool`；看物铭牌仍可用全 bank**

**步骤 4：测试通过**

---

### 任务 1.5：挖单词方块先出题再记掌握

**文件：**
- 修改：`prj/games/blocklegend/data/words.js` `collectWordBlock`
- 修改：`prj/games/blocklegend/game.js` `finishBreak`
- 测试：`tests/blocklegend.test.mjs`

**步骤 1：** 断言 `collectWordBlock` 默认 `pendingQuiz: true` 且 **不** 把 id 写入 `learnedIds`；答对后再 `commitWordBlock`。

**步骤 2：** `finishBreak` 挖到 word 时调用现有 `openQuiz`，答对才给金币/HP 并 `recordWordAnswer`。

**步骤 3：** 测试通过

---

### 任务 1.6：Boss 学习阶段文案（BL-07 最小版）

**文件：**
- 修改：`prj/games/blocklegend/index.html` Boss HUD
- 修改：`prj/games/blocklegend/game.js` `paintBoss`
- 测试：源码断言存在阶段名

三阶段文案：`识别` / `回忆` / `破罩输出`。用已有 `boss.state`（shielded / broken）映射：

- shielded 且 shield 满：识别
- shielded 且 shield 已削：回忆
- broken：破罩输出

不要新做 Boss 模型。

---

### 任务 1.7：结算页说人话

**文件：**
- 修改：`prj/games/blocklegend/data/levels.js` `buildSettlement`
- 修改：`prj/games/blocklegend/game.js` 结算填充

结算至少三行：本关新词数、待复习词、阳光去向。复用现有 `#settle-gain` / `#settle-progress` / `#settle-next`。

---

## Phase 1 验收

- 首局 HUD 有唯一 `quest-goal`
- 答错先揭示再给一次重试，不立刻记 wrong
- 第 1 关出题池 5 词
- 挖金色词块不再直接 `recordWordAnswer(..., true)`
- `tests/blocklegend.test.mjs` 全绿

不要在本阶段做麦克风。
